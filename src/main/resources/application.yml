quarkus:
  log:
    level: INFO
  http:
    cors:
      ~: true
    port: 8123
  datasource:
    db-kind: mysql
    username: root
    password: ""
    jdbc:
      url: jdbc:mysql://localhost:3306/shop_app_db
  hibernate-orm:
    database:
      generation:
        ~: none